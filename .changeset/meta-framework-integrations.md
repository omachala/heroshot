---
'heroshot': minor
---

Add SvelteKit, Next.js, and Nuxt integrations. New Svelte display component, Next.js webpack plugin with `withHeroshot()` config wrapper, and `'use client'` entry point for React Server Components. Vite plugin now supports SvelteKit's `static/` directory.
